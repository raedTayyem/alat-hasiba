// Convert number to string (kept for compatibility with calendar calculators)
export const toArabicDigits = (num: number | string): string => {
  return String(num);
};

export const convertToArabicDigits = toArabicDigits; 